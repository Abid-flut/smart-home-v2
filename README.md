# smart-home-v2

Smart Home V2 – Clean Architecture Flutter Application (In Progress)





\## Architecture Rules



\- AuthProvider manages authentication state only.

\- DeviceProvider manages device domain state only.

\- WeatherProvider manages weather domain state only.

\- No business logic inside UI.

\- No networking inside widgets.

\- Clear separation between domains.

